[package]
name = "capture-core"
version = "0.1.0"
edition = "2021"
description = "Shared capture core for ashot"
license = "BSD-3-Clause"

[dependencies]
serde = { version = "1", features = ["derive"] }

